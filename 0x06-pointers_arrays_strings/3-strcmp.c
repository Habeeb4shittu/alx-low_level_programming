#include <string.h>
#include <stdio.h>
#include "main.h"
/**
* _strcmp - comparation of strings
* @s1: string 1
* @s2: string 2
* Return: always zero
*/
int _strcmp(char *s1, char *s2)
{
int result;
result = strcmp(s1, s2);
return (result);
}
